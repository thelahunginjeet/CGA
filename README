README file for CGA project.

branch	: feature-sqlitedb
purpose : adding a sqlite database back-end to record functions and other metadata

FEATURES / FIXES:
	- made generations a unique primary key for the cgaruns table; hence re-running will
	OVERWRITE data from the previous run
	- simulation metadata is only written at generations = 0

